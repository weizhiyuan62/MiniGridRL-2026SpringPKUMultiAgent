% !TEX program = xelatex
\documentclass[UTF8,a4paper,fontset=fandol]{ctexart}
\usepackage{geometry}
\usepackage{graphicx} % Required for inserting images
\usepackage{amsmath, amssymb}
\usepackage{hyperref}
\geometry{margin=2.5cm}
\hypersetup{colorlinks=true,linkcolor=blue,urlcolor=blue}
\ctexset{
  section = {
    format = \Large\bfseries\raggedright
  }
}

\title{个人课题(选题3)汇报}
\author{姓名：魏知原}
\date{学号：2300012875}

\begin{document}
\pagestyle{plain}
\maketitle

\section{任务1：MDP形式化与环境分析}

本项目使用 MiniGrid-Dynamic-Obstacles 系列环境，并按要求使用
\texttt{SymbolicObsWrapper} 对环境进行包装。包装后，观测仍然是字典形式，主要字段包括
\texttt{image}、\texttt{direction} 和 \texttt{mission}。其中 \texttt{image} 不再是原始
RGB 局部视野，而是完整网格的符号化表示，形状为
\[
    (N, N, 3),
\]
其中 \(N\) 是网格边长。每个格子的三元组可理解为
\[
    (x, y, object\_id),
\]
即该格子的横坐标、纵坐标和对象编号。包装器会将智能体所在格子的对象编号标记为
\texttt{agent}，因此该观测可以直接反映完整网格中墙、空地、目标、移动障碍物和智能体的
位置。环境任务文本固定为 ``get to the green goal square''，因此在本项目的 MDP 分析中，
\texttt{mission} 不作为额外变化状态处理。

\subsection{状态空间 \(S\)}

从马尔可夫性的角度，一个足够描述环境动态的紧凑状态可以写成
\[
    s = (x_a, y_a, d_a, p_1, p_2, \ldots, p_k),
\]
其中 \((x_a, y_a)\) 是智能体位置，\(d_a \in \{0,1,2,3\}\) 是智能体朝向，
\(p_i\) 是第 \(i\) 个动态障碍物的位置，\(k\) 是障碍物数量。边界墙的位置固定，目标格固定在
右下角内侧 \((N-2, N-2)\)，因此不需要作为变化变量计入状态。

为了估计理论状态空间大小，令
\[
    M = (N-2)^2 - 1
\]
表示去除边界墙并排除目标格之后，可由智能体或障碍物占据的内部非目标格数量。若把障碍物
视为同质对象且不允许多个对象占据同一格，则非终止状态空间大小的近似上界为
\[
    |S| \approx 4 \cdot M \cdot \binom{M-1}{k}.
\]
这里的 \(4\) 来自智能体的四个朝向，\(M\) 是智能体位置数，\(\binom{M-1}{k}\) 是在智能体
所在格之外选择 \(k\) 个障碍物位置的组合数。

\begin{center}
\begin{tabular}{c|c|c|c}
环境规模 & 障碍物数量 \(k\) & \(M\) & 估计状态数 \\
\hline
5x5 & 2 & 8 & 672 \\
8x8 & 4 & 35 & 6,492,640 \\
16x16 & 8 & 195 & 33,533,784,962,552,160 \\
\end{tabular}
\end{center}

\texttt{MiniGrid-Dynamic-Obstacles-Random-5x5-v0} 与普通 5x5 环境的网格规模和障碍物数量相同，
因此理论状态空间规模相同；区别主要体现在初始状态分布上，随机版本会随机化智能体初始位置
和方向。移动障碍物显著扩大了状态空间：如果没有障碍物，状态主要由智能体位置和朝向组成；
加入多个动态障碍物后，状态还必须包含障碍物的位置组合，状态数随网格规模和障碍物数量呈组合
式增长。这正是表格型 Q-Learning 在大网格中面临维度灾难的根本原因。

\subsection{动作空间 \(A\)}

Dynamic-Obstacles 环境将动作空间限制为三个离散动作：
\[
    A = \{\texttt{left}, \texttt{right}, \texttt{forward}\}.
\]
对应编号为：
\begin{center}
\begin{tabular}{c|c|c}
编号 & 动作 & 含义 \\
\hline
0 & \texttt{left} & 向左旋转 \\
1 & \texttt{right} & 向右旋转 \\
2 & \texttt{forward} & 向当前朝向前进一格 \\
\end{tabular}
\end{center}

MiniGrid 通用动作集合中还包含 \texttt{pickup}、\texttt{drop}、\texttt{toggle} 和
\texttt{done}，但在本环境中这些动作不可用。

\subsection{奖励函数 \(R\)}

该环境奖励较稀疏。普通移动或转向通常得到 \(0\) 奖励；当智能体成功到达目标格时，回合终止
并获得带时间衰减的正奖励：
\[
    R_{\text{success}} = 1 - 0.9 \cdot \frac{\texttt{step\_count}}{\texttt{max\_steps}}.
\]
因此越早到达目标，奖励越高。若智能体尝试向前进入动态障碍物或墙所在格，会得到 \(-1\)
惩罚并终止回合。若达到最大步数仍未成功，则回合被截断，失败奖励为 \(0\)。整体来看，奖励
信号主要出现在成功、碰撞和超时等事件上，因此属于稀疏奖励问题。

\subsection{转移概率 \(P\)}

在给定当前状态和动作时，智能体自身的转向和前进一步规则是确定性的；例如
\texttt{left} 和 \texttt{right} 只改变朝向，\texttt{forward} 尝试沿当前朝向移动一格。
但是整个环境转移并非确定性，因为每一步中动态障碍物会在其当前位置附近的 \(3 \times 3\)
区域内重新采样可用位置。该采样依赖环境随机数，并且可能受到墙、目标、智能体和其他障碍物
占位情况的限制。因此总体转移概率应写作
\[
    P(s' \mid s, a),
\]
而不是确定性映射 \(s' = f(s,a)\)。

对于普通 5x5、8x8 和 16x16 版本，初始位置通常是固定的，但回合内部仍然因为动态障碍物
移动而具有随机性；对于 \texttt{Random-5x5-v0}，初始状态分布本身也具有随机性。这意味着
PPO 等函数逼近方法不仅要学习避障策略，还需要对不同初始状态具有一定泛化能力，而表格型
Q-Learning 则需要为大量离散状态分别估计动作价值。

\subsection{小结}

任务一的分析说明，MiniGrid-Dynamic-Obstacles 可以形式化为一个有限但规模快速膨胀的离散
MDP。小规模环境中表格型 Q-Learning 尚有可能覆盖较多状态，但当环境扩展到 16x16 且包含
8 个动态障碍物时，理论状态空间已经达到约 \(3.35 \times 10^{16}\) 量级。该结果预示了后续
实验中表格方法可能遭遇严重样本效率和存储效率问题，也为比较 PPO 的函数逼近和泛化能力
提供了依据。

\section{任务2：算法实现、超参数与单次实验总结}

本项目实现并比较了表格型 Q-Learning 与 PPO-Clip。二者都使用
\texttt{SymbolicObsWrapper} 后的符号观测，但状态利用方式不同。Q-Learning 将完整 object
layer 与朝向拼成离散 key；PPO 使用一个小型 CNN actor-critic，对 \(3 \times N \times N\)
的符号图像通道进行卷积编码，并将朝向 one-hot 后拼接到特征中。

\subsection{超参数设置}

Q-Learning 使用学习率 \(\alpha=0.2\)，折扣因子 \(\gamma=0.99\)，epsilon-greedy 探索从
\(1.0\) 线性衰减到 \(0.05\)，衰减步数为 40,000。Q 表初始值均为 0。5x5 与 Random-5x5
训练 50,000 steps；8x8 的多 seed 实验训练 100,000 steps；16x16 的最新多 seed 实验
训练 130,000 steps，另有单 seed 200,000 steps 的补充观察。

PPO-Clip 使用学习率 \(3\times 10^{-4}\)，\(\gamma=0.99\)，GAE 参数
\(\lambda=0.95\)，clip 系数 0.2，value loss 系数 0.5，entropy 系数默认 0.01。
每次 rollout 长度为 1024，minibatch size 为 256，每个 rollout 更新 4 个 epoch。
CNN 通道数设为 16，隐藏层宽度设为 64，以便调试和控制计算量。PPO 使用原始环境奖励，
未加入额外 reward shaping；我也尝试过将 entropy 系数调大到 0.05、0.1、0.3，但并没有解决
成功率塌缩的问题。

\subsection{单次实验现象}

在 seed 23 的单次实验中，Q-Learning 在两个 5x5 环境中表现良好：普通 5x5 最后 100 回合
成功率达到 91.00\%，Random-5x5 最后 100 回合成功率达到 99.00\%。对应的 Q-table size
分别约为 646 和 672，说明小环境中的可访问状态基本被覆盖。

但在大棋盘中，Q-Learning 失败明显。8x8 在 200,000 steps 下最终 Q-table size 达到
160,557，最后 100 回合成功率为 0；16x16 的 Q-table size 达到 199,792，且未出现成功回合。
这说明表格方法在大棋盘上持续遇到新状态，无法充分复用已有估计。

PPO-Clip 的主要现象是：优化指标可以变得稳定，但任务成功率仍然接近 0。debug log 显示，
PPO 初期会尝试 \texttt{forward}，但很快学到减少前进动作以避免碰撞；后期策略多表现为
左右转并拖到 timeout。也就是说，PPO 并不是完全没有学习，而是陷入了一个局部最优：在稀疏
正奖励和碰撞负反馈下，原地左右转比主动前进更容易获得“看起来不太差”的回报。将 entropy
系数调大后，探索稍有变化，但仍未形成稳定到达目标的策略。

\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.75\linewidth]{images/ppo_5x5_timeout_policy.png}
    \caption{PPO-Clip 在 5x5 上容易学到避免碰撞但不到达目标的 timeout policy。}
\end{figure}

\begin{figure}[htbp]
    \centering
    \begin{minipage}{0.32\linewidth}
        \centering
        \includegraphics[width=\linewidth]{images/ppo_5x5_entropy_0p05.png}
        \caption{PPO entropy=0.05}
    \end{minipage}
    \hfill
    \begin{minipage}{0.32\linewidth}
        \centering
        \includegraphics[width=\linewidth]{images/ppo_5x5_entropy_0p1.png}
        \caption{PPO entropy=0.1}
    \end{minipage}
    \hfill
    \begin{minipage}{0.32\linewidth}
        \centering
        \includegraphics[width=\linewidth]{images/ppo_5x5_entropy_0p3.png}
        \caption{PPO entropy=0.3}
    \end{minipage}
\end{figure}

\section{任务3：多随机种子下的比较分析}

任务三中，我使用随机种子 \(\{1,23,31,42,97\}\) 进行重复实验，并对回合回报曲线在相同
timestep 网格上取平均。5x5 与 Random-5x5 使用 50,000 steps，8x8 使用 100,000 steps，
16x16 使用最新的 130,000 steps 结果。图中曲线为 5 个随机种子的均值，并绘制标准差阴影。

\subsection{学习曲线}


\begin{figure}[htbp]
    \centering
    \begin{minipage}{0.48\linewidth}
        \centering
        \includegraphics[width=\linewidth]{images/mean_return_5x5.png}
        \caption{5x5 平均学习曲线}
    \end{minipage}
    \hfill
    \begin{minipage}{0.48\linewidth}
        \centering
        \includegraphics[width=\linewidth]{images/mean_return_random_5x5.png}
        \caption{Random-5x5 平均学习曲线}
    \end{minipage}
\end{figure}

\begin{figure}[htbp]
    \centering
    \begin{minipage}{0.48\linewidth}
        \centering
        \includegraphics[width=\linewidth]{images/mean_return_8x8.png}
        \caption{8x8 平均学习曲线}
    \end{minipage}
    \hfill
    \begin{minipage}{0.48\linewidth}
        \centering
        \includegraphics[width=\linewidth]{images/mean_return_16x16.png}
        \caption{16x16 平均学习曲线}
    \end{minipage}
\end{figure}

\subsection{定量结果}

\begin{center}
\begin{tabular}{c|c|r|r|r|r}
算法 & 环境 & steps & 全程成功率 & 最后100成功率 & 最后100平均回报 \\
\hline
Q-Learning & 5x5 & 50,000 & 29.10\% & 89.60\% & 0.684 \\
Q-Learning & Random-5x5 & 50,000 & 56.50\% & 96.20\% & 0.870 \\
Q-Learning & 8x8 & 100,000 & 0.06\% & 0.00\% & -0.806 \\
Q-Learning & 16x16 & 130,000 & 0.00\% & 0.00\% & -0.972 \\
PPO-Clip & 5x5 & 50,000 & 0.38\% & 0.00\% & -0.032 \\
PPO-Clip & Random-5x5 & 50,000 & 7.11\% & 0.20\% & -0.010 \\
PPO-Clip & 8x8 & 100,000 & 0.05\% & 0.00\% & -0.016 \\
PPO-Clip & 16x16 & 130,000 & 0.00\% & 0.00\% & -0.076 \\
\end{tabular}
\end{center}

\subsection{维度灾难}

当环境从 5x5 扩展到 16x16 时，表格型 Q-Learning 的表现迅速恶化。5x5 中平均最终
Q-table size 约为 647，Random-5x5 为 672，与理论状态规模同量级，因此每个状态可以被
多次访问并更新；而 8x8 训练 100,000 steps 后，平均 Q-table size 已经达到约 90,116。
16x16 在最新的 130,000 steps 实验中，平均 Q-table size 进一步达到约 129,848，几乎等于
环境交互步数。这意味着大棋盘中 Q-Learning 仍在不断发现新状态，无法形成稳定价值估计。

从成功率看，Q-Learning 在 5x5 最后 100 回合平均成功率为 89.60\%，Random-5x5 为
96.20\%；但在 8x8 和 16x16 的最后 100 回合成功率均为 0。即使将 16x16 的训练步数增加到
130,000，五个随机种子仍然没有任何成功回合，Q-table 只是继续增长。这正体现了维度灾难：
状态空间的组合式增长使表格方法的样本效率急剧下降。

\subsection{泛化能力}

Random-5x5-v0 随机化了智能体初始位置和方向。Q-Learning 在该环境中仍表现很好，最后 100
回合平均成功率达到 96.20\%。原因是 Random-5x5 的状态空间仍然很小，Q-table 最终覆盖到
672 个状态，表格方法通过枚举状态也能处理随机初始分布。

PPO 理论上具有函数逼近带来的泛化能力，但本实验中的 PPO-Clip 并没有充分体现这一优势。
在 Random-5x5 中，PPO 全程平均成功率为 7.11\%，高于普通 5x5 的 0.38\%，说明随机初始
位置确实带来了更多成功样本和更丰富探索；但最后 100 回合成功率仍只有 0.20\%。这表明 PPO
并未稳定学到目标导向策略，而是倾向于学习避免碰撞、拖到超时的保守策略。尤其是在动作分布
收敛后，策略常表现为连续选择 \texttt{left}/\texttt{right}，通过改变朝向来消耗步数，而
不是利用已有视觉特征规划到达 goal。

\subsection{计算效率与样本效率}

在小规模环境中，Q-Learning 的样本效率最高。5x5 中它在 50,000 steps 内达到接近 90\% 的
末尾成功率；Random-5x5 中甚至接近 96\%。PPO 在同样步数下虽然可以把平均回报从频繁碰撞的
负值提高到接近 0，但没有转化为成功率提升，因此从任务成功角度看样本效率较低。

在大规模环境中，两种算法都没有解决任务，但失败方式不同。Q-Learning 的失败主要来自状态
空间爆炸，训练越久 Q-table 越大；PPO 的失败主要来自稀疏奖励下的局部最优，它能学会减少
碰撞和延长回合，但不一定到达目标。更具体地说，PPO 学到的是“少做危险的 forward，多做
左右转”的策略；这会提高平均回报、降低碰撞频率，却牺牲了真正的成功率。PPO 在 8x8 和
16x16 在 130,000 steps 后最后 100 回合平均回报约为 \(-0.076\)，最后 100 回合平均长度约为
982 steps，看似优于 Q-Learning 的大负回报，但这主要代表 timeout，而不是成功完成任务。

\subsection{总结}

综合来看，表格型 Q-Learning 在小状态空间中简单、高效、稳定，但随环境规模扩大迅速遭遇
维度灾难。PPO-Clip 具备函数逼近能力，并在随机初始位置下能获得更多早期成功样本，但在本
实验的稀疏奖励 Dynamic-Obstacles 任务中，最终未能形成稳定成功策略。后续若要提升 PPO，
可能需要引入更合理的辅助奖励，因为目前的奖励设计过于稀疏，导致 PPO 学习到的策略过于保守，难以突破局部最优。


\end{document}
